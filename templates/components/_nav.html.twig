<!-- Wrapper -->
<div id="wrapper">

    <!-- Header -->
    <header id="header" class="alt">
        <a href="{{ path('index') }}" class="logo"><strong>Forty</strong> <span>by HTML5 UP</span></a>
        <nav>
            <a href="#menu">Menu</a>
        </nav>
    </header>

    <!-- Menu -->
    <nav id="menu">
        <ul class="links">
            <li><a href="{{ path('index') }}">Home</a></li>
            <li><a href="{{ path('landing') }}">Landing</a></li>
            <li><a href="{{ path('generic') }}">Generic</a></li>
            <li><a href="{{ path('elements') }}">Elements</a></li>
            <li><a href="{{ path('contact') }}">Contact</a></li>
        </ul>
        <ul class="actions stacked">
            <li><a href="#" class="button primary fit">Get Started</a></li>

            <!-- Ajout des fonctions Inscription et connexion
            {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                <li>
                    <a href="{{ path('security_deconnexion') }}">
                        Déconnexion
                    </a>
                </li>
            {% else %}
                <li {{ active == 'inscription' ? 'class="current"' }}>
                    <a href="{{ path('membre_inscription') }}">
                        Inscription
                    </a>
                </li>
                <li {{ active == 'connexion' ? 'class="current"' }}>
                    <a href="{{ path('security_connexion') }}">
                        Connexion
                    </a>
                </li>
            {% endif %}

            <!--<li><a href="#" class="button fit">Log In</a></li>-->
        </ul>
    </nav>

</div>